# Tasks — API-Football Integration (Phase 9)

Source of truth for this phase. Check a box only when implemented AND tests pass.

## Build

- [ ] A1  `data/apifootball.py` — league id map + parse_standings + compute_league_avg (TDD)
- [ ] A2  `ApiFootballProvider` — headers (key), _load_league/list_teams/get_match, repr mask (TDD)
- [ ] A3  `app.py` — "API-Football (live)" source: API key + season inputs, gated fetch + AppTest
- [ ] A4  README API-Football section + roadmap update; full suite green

## Done criteria
- All boxes checked
- `pytest` green (existing + new)
- No open CRITICAL/HIGH review findings
- App shows API key + season inputs in API-Football (live) mode
